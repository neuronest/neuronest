repository_variables:
  # general repository
  - REPOSITORY_NAME=functional-detect-anything
  - REPOSITORY_CODE=func-da
  - MAIN_TERRAFORM_MODULE_NAME=functional_detect_anything
  - REPOSITORIES_DEPENDENCIES=detect-anything,model-instantiator
multi_instance_resource_names: []
other_variables:
  branch:
    default:
    # GCP project
    - PROJECT_ID=${FUNCTIONAL_SERVICES_PROJECT_ID}
    - PROJECT_CODE=${FUNCTIONAL_SERVICES_PROJECT_CODE}
